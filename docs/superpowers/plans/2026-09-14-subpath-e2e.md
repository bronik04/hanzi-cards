# Подпутевой стенд Playwright — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Завести второй стенд Playwright, который собирает приложение с `DEPLOY_BASE=/hanzi-cards/` — тем же base, под которым GitHub Pages отдаёт продакшен, — и гоняет по нему дымовой набор из четырёх проверок.

**Architecture:** Два стенда разведены по трём осям: свой конфиг, свой порт, свой `outDir`. Обычный стенд — `playwright.config.ts` / `e2e/` / `dist/` / порт 4173 / base `/`. Подпутевой — `playwright.subpath.config.ts` / `e2e-subpath/` / `dist-subpath/` / порт 4174 / base `/hanzi-cards/`. Поскольку `testDir` основного конфига указывает на `e2e/`, новая соседняя папка ему не видна, и `npm run test:e2e` не меняется ни на строку.

**Tech Stack:** Playwright, Vite, `vite-plugin-pwa` (workbox), GitHub Actions.

**Спецификация:** [docs/superpowers/specs/2026-09-14-subpath-e2e-design.md](../specs/2026-09-14-subpath-e2e-design.md)

## Global Constraints

- Подпуть ровно один: `/hanzi-cards/`, с ведущим и замыкающим слэшем. Записан
  один раз в `e2e-subpath/base.ts`, все остальные файлы импортируют его оттуда.
- Порт подпутевого стенда — `4174`. Порт 4173 занят обычным стендом.
- Папка сборки подпутевого стенда — `dist-subpath/`. Никогда `dist/`.
- `playwright.config.ts`, `e2e/**`, `vite.config.ts` и `.github/workflows/deploy.yml`
  не меняются. Если задача требует их править — задача поставлена неверно.
- В тестах навигация только через `'./'`. `page.goto('/')` при `baseURL` с путём
  разрешается относительно origin и уводит в корень домена.
- Интерфейс приложения на русском. Идентификаторы, имена файлов и сообщения
  коммитов — на английском.
- Prettier: одинарные кавычки, `printWidth: 100`, `trailingComma: "all"`.
- ESLint-конфиг проекта включает `@typescript-eslint/no-non-null-assertion`
  (из `tseslint.configs.recommended`). Оператор `!` использовать нельзя —
  сужать тип через `if (x === null) throw new Error(...)`.
- ESLint-конфиг проекта включает `@typescript-eslint/consistent-type-imports`:
  импорт типов только через `import type`.
- Каждая задача заканчивается коммитом в стиле Conventional Commits.
- Сообщения коммитов заканчиваются строкой
  `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.

## File Structure

| Файл | Ответственность |
|------|-----------------|
| `e2e-subpath/base.ts` | Единственное место, где записаны подпуть, порт и адрес стенда |
| `playwright.subpath.config.ts` | Конфиг стенда: сборка с `DEPLOY_BASE`, свой `outDir`, свой порт |
| `e2e-subpath/smoke.spec.ts` | Четыре дымовые проверки продакшен-конфигурации |
| `package.json` | Скрипт `test:e2e:subpath` |
| `.gitignore` | `dist-subpath/` |
| `tsconfig.json` | `e2e-subpath` и новый конфиг в `include` — список там поимённый |
| `eslint.config.js` | `dist-subpath` в `ignores` |
| `.github/workflows/ci.yml` | Джоба `e2e-subpath` |

Хелперы `serviceWorkerActive`, `serviceWorkerControlling` и фикстура `SHORT_TABLE`
берутся из существующего `e2e/fixtures.ts` импортом `'../e2e/fixtures'`.
Дублировать их нельзя.

---

### Task 1: Стенд и первая проверка — загрузка без ответов 4xx

Стенд без единой проверки нечем проверить, поэтому конфигурация и первый тест
едут одной задачей. Её результат: `npm run test:e2e:subpath` собирает приложение
под подпутём, поднимает сервер и гоняет один зелёный тест.

**Files:**
- Create: `e2e-subpath/base.ts`
- Create: `playwright.subpath.config.ts`
- Create: `e2e-subpath/smoke.spec.ts`
- Modify: `package.json` (блок `scripts`)
- Modify: `.gitignore`
- Modify: `tsconfig.json` (массив `include`)
- Modify: `eslint.config.js` (массив `ignores`)

**Interfaces:**
- Produces: `e2e-subpath/base.ts` экспортирует
  `BASE: string` (`'/hanzi-cards/'`), `PORT: number` (`4174`),
  `APP_URL: string` (`'http://localhost:4174/hanzi-cards/'`).
  Задачи 2 и 3 импортируют `BASE` и `APP_URL` из этого модуля.
- Produces: npm-скрипт `test:e2e:subpath`, который использует задача 4.

- [ ] **Step 1: Создать модуль с подпутём**

Создать `e2e-subpath/base.ts`:

```ts
/** Подпуть, под которым GitHub Pages отдаёт приложение:
 *  https://bronik04.github.io/hanzi-cards/ */
export const BASE = '/hanzi-cards/';

/** Свой порт: 4173 занят обычным стендом. */
export const PORT = 4174;

/** Адрес приложения на стенде — он же baseURL тестов. */
export const APP_URL = `http://localhost:${PORT}${BASE}`;
```

- [ ] **Step 2: Написать конфиг стенда**

Создать `playwright.subpath.config.ts`:

```ts
import { defineConfig } from '@playwright/test';
import { APP_URL, BASE, PORT } from './e2e-subpath/base';

// Второй стенд: приложение собрано с тем же base, с которым его отдаёт
// GitHub Pages. От base зависят пути к ассетам, start_url и scope манифеста,
// scope регистрации service worker и список URL в прекеше workbox — всё то,
// что обычный стенд с base '/' не проверяет вовсе.
export default defineConfig({
  testDir: './e2e-subpath',
  fullyParallel: true,
  forbidOnly: process.env.CI === 'true',
  retries: process.env.CI === 'true' ? 1 : 0,
  use: { baseURL: APP_URL },
  webServer: {
    // Свой outDir: обычный стенд собирает в dist/, и запущенный рядом
    // `npm run preview` иначе начал бы молча отдавать файлы с чужим base.
    // vite build напрямую, а не npm run build: последний добавляет
    // tsc --noEmit, который в CI уже отработал в джобе check.
    command: `vite build --outDir dist-subpath && vite preview --outDir dist-subpath --port ${PORT}`,
    // Переменная живёт здесь, а не в npm-скрипте: скрипт без неё собрал бы
    // корневой base в подпутевую папку и врал бы собственному имени.
    env: { DEPLOY_BASE: BASE },
    url: APP_URL,
    reuseExistingServer: process.env.CI !== 'true',
    timeout: 120_000,
  },
});
```

- [ ] **Step 3: Написать первый тест**

Создать `e2e-subpath/smoke.spec.ts`:

```ts
import { expect, test } from '@playwright/test';
import { BASE } from './base';

test('приложение под подпутём грузится без ответов 4xx', async ({ page }) => {
  const failed: string[] = [];
  page.on('response', (response) => {
    // Заодно ловим 5xx: это ничего не стоит.
    if (response.status() >= 400) failed.push(`${response.status()} ${response.url()}`);
  });

  const response = await page.goto('./');
  if (response === null) throw new Error('стенд не ответил на запрос страницы');

  // baseURL здесь с путём, и goto('/') ушёл бы в корень домена, а не в
  // подкаталог. vite preview отвечает на корень редиректом 302 на подпуть,
  // так что такая ошибка была бы зелёной и локально, и в CI, — а на Pages
  // в корне домена лежит чужой сайт. Требуем, чтобы захода через корень не было.
  expect(response.request().redirectedFrom()).toBeNull();
  expect(new URL(page.url()).pathname).toBe(BASE);

  // Ждём отрисованный экран, а не событие load: иначе проверка сойдётся
  // раньше, чем приедут ассеты, и пропустит их 404.
  await expect(page.getByRole('heading', { name: 'Вставьте таблицу со словами' })).toBeVisible();
  expect(failed).toEqual([]);
});
```

- [ ] **Step 4: Убедиться, что тест пока не запускается**

Run: `npm run test:e2e:subpath`

Expected: FAIL — `npm error Missing script: "test:e2e:subpath"`.
Скрипта ещё нет, это и проверяем.

- [ ] **Step 5: Добавить npm-скрипт**

В `package.json`, в блок `scripts`, сразу после строки `"test:e2e": "playwright test",`:

```json
    "test:e2e:subpath": "playwright test --config playwright.subpath.config.ts",
```

- [ ] **Step 6: Запустить тест**

Run: `npm run test:e2e:subpath`

Expected: PASS — `1 passed`. Сборка уходит в `dist-subpath/`, сервер поднимается
на `http://localhost:4174/hanzi-cards/`.

Если падает на `redirectedFrom` — в тесте `goto('/')` вместо `goto('./')`.
Если падает таймаутом webServer — порт 4174 занят; проверить `lsof -i :4174`.

- [ ] **Step 7: Закрыть новую папку сборки от git**

В `.gitignore`, сразу после строки `dist/`, добавить:

```
dist-subpath/
```

Строка `dist/` эту папку не покрывает.

- [ ] **Step 8: Добавить новые файлы в typecheck**

В `tsconfig.json` заменить строку `include` целиком на:

```json
  "include": [
    "src",
    "e2e",
    "e2e-subpath",
    "vite.config.ts",
    "playwright.config.ts",
    "playwright.subpath.config.ts"
  ]
```

Список там поимённый: без этой правки `npm run typecheck` новые файлы не увидит.

- [ ] **Step 9: Закрыть новую папку сборки от линтера**

В `eslint.config.js`, в массив `ignores` первого объекта, после `'dist',` добавить:

```js
      'dist-subpath',
```

Иначе `eslint .` пойдёт по собранным бандлам в `dist-subpath/`.

- [ ] **Step 10: Прогнать линтер и typecheck**

Run: `npm run lint && npm run typecheck`

Expected: обе команды завершаются без вывода ошибок, код возврата 0.

- [ ] **Step 11: Убедиться, что обычный прогон не задет**

Run: `npm run test:e2e`

Expected: PASS, ровно столько же тестов, сколько было до задачи (6).
Ни один тест из `e2e-subpath/` в этот список попасть не должен.

- [ ] **Step 12: Commit**

```bash
git add e2e-subpath/base.ts playwright.subpath.config.ts e2e-subpath/smoke.spec.ts package.json .gitignore tsconfig.json eslint.config.js
git commit -F - <<'MSG'
test: add an e2e stand that builds with the Pages base

Обычный стенд проверяет приложение только при base '/'. В продакшене
GitHub Pages отдаёт его из подкаталога, и от base зависят пути к ассетам,
манифест и scope service worker — всё то, из-за чего офлайн уже один раз
молча не работал.

Второй стенд собирает приложение с DEPLOY_BASE=/hanzi-cards/ в отдельный
outDir и на отдельном порту, поэтому обычный прогон не меняется.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

### Task 2: Проверки манифеста и scope service worker

Две статические проверки того, что base доехал до метаданных приложения.
Едут одной задачей: обе отвечают на один вопрос — «описывает ли конфигурация
подпуть», — и падают от одной и той же причины.

**Files:**
- Modify: `e2e-subpath/smoke.spec.ts` (дописать два теста в конец)

**Interfaces:**
- Consumes: `BASE` и `APP_URL` из `e2e-subpath/base.ts` (задача 1).
- Consumes: `serviceWorkerActive(page: Page): Promise<void>` из `e2e/fixtures.ts`.
  Ждёт, пока `registration.active.state` станет `'activated'`, таймаут 15 с.

- [ ] **Step 1: Расширить импорты**

В `e2e-subpath/smoke.spec.ts` заменить блок импортов на:

```ts
import { expect, test } from '@playwright/test';
import { serviceWorkerActive } from '../e2e/fixtures';
import { APP_URL, BASE } from './base';
```

- [ ] **Step 2: Дописать проверку манифеста**

В конец `e2e-subpath/smoke.spec.ts`:

```ts
test('манифест лежит под подпутём и описывает его в start_url и scope', async ({ page }) => {
  await page.goto('./');

  // Атрибут, а не свойство .href: свойство браузер уже разрешил в абсолютный
  // URL, и подмена base читается в нём хуже.
  const href = await page.locator('link[rel="manifest"]').getAttribute('href');
  expect(href).toBe(`${BASE}manifest.webmanifest`);

  const response = await page.request.get(`${BASE}manifest.webmanifest`);
  expect(response.status()).toBe(200);

  const manifest = (await response.json()) as { start_url?: string; scope?: string };
  // Неверный start_url — это установленное приложение, которое открывается
  // не туда. На глаз незаметно: первая же навигация выглядит нормально.
  expect(manifest.start_url).toBe(BASE);
  expect(manifest.scope).toBe(BASE);
});
```

- [ ] **Step 3: Дописать проверку scope service worker**

В конец `e2e-subpath/smoke.spec.ts`:

```ts
test('service worker зарегистрирован со scope подпути', async ({ page }) => {
  await page.goto('./');
  await serviceWorkerActive(page);

  const registration = await page.evaluate(async () => {
    const current = await navigator.serviceWorker.getRegistration();
    return {
      scope: current?.scope ?? null,
      scriptURL: current?.active?.scriptURL ?? null,
    };
  });

  // Хелпер serviceWorkerControlling проверяет только вхождение 'sw.js', и под
  // корневым base такая проверка тоже зелёная. Здесь нужен полный адрес:
  // именно scope решает, какие навигации service worker вообще перехватывает.
  expect(registration.scope).toBe(APP_URL);
  expect(registration.scriptURL).toBe(`${APP_URL}sw.js`);
});
```

- [ ] **Step 4: Запустить обе проверки**

Run: `npm run test:e2e:subpath`

Expected: PASS — `3 passed`.

Если проверка манифеста падает на `href` со значением `null` — селектор
`link[rel="manifest"]` не нашёл элемент; проверить, что `VitePWA` в
`vite.config.ts` по-прежнему генерирует манифест.

- [ ] **Step 5: Прогнать линтер и typecheck**

Run: `npm run lint && npm run typecheck`

Expected: обе команды без ошибок, код возврата 0.

- [ ] **Step 6: Commit**

```bash
git add e2e-subpath/smoke.spec.ts
git commit -F - <<'MSG'
test: check the manifest and service worker scope under the Pages base

start_url и scope vite-plugin-pwa выводит из base, а scope решает, какие
навигации service worker перехватывает. Неверные значения не видны на глаз:
первая навигация выглядит нормально, ломается установленное приложение.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

### Task 3: Проверка офлайна под подпутём

**Files:**
- Modify: `e2e-subpath/smoke.spec.ts` (дописать тест в конец)

**Interfaces:**
- Consumes: `serviceWorkerActive(page: Page): Promise<void>` и
  `serviceWorkerControlling(page: Page): Promise<void>` из `e2e/fixtures.ts`.
  Второй ждёт, пока `navigator.serviceWorker.controller.scriptURL` перестанет
  быть пустым, таймаут 10 с.
- Consumes: `SHORT_TABLE: string` из `e2e/fixtures.ts` — две строки вида
  `иероглиф<TAB>пиньинь<TAB>перевод`, дающие две карточки.

- [ ] **Step 1: Расширить импорты**

В `e2e-subpath/smoke.spec.ts` заменить блок импортов на:

```ts
import { expect, test } from '@playwright/test';
import { SHORT_TABLE, serviceWorkerActive, serviceWorkerControlling } from '../e2e/fixtures';
import { APP_URL, BASE } from './base';
```

- [ ] **Step 2: Дописать проверку офлайна**

В конец `e2e-subpath/smoke.spec.ts`:

```ts
// Без промежуточной перезагрузки, как и в e2e/offline.spec.ts: она
// замаскировала бы регресс clientsClaim — тест проходил бы и без него.
// Под подпутём проверка вдобавок ловит прекеш: workbox прерывает установку
// service worker, если хоть один URL прекеша ответил 404.
test('приложение под подпутём работает без сети с первого визита', async ({ page, context }) => {
  await page.goto('./');
  await serviceWorkerActive(page);
  await serviceWorkerControlling(page);

  await context.setOffline(true);
  await page.reload();

  await expect(page.getByRole('heading', { name: 'Вставьте таблицу со словами' })).toBeVisible();
  await page.getByLabel('Таблица со словами').fill(SHORT_TABLE);
  await expect(page.getByText('Добавлено 2 карточки')).toBeVisible();
});
```

- [ ] **Step 3: Запустить весь набор**

Run: `npm run test:e2e:subpath`

Expected: PASS — `4 passed`.

- [ ] **Step 4: Прогнать линтер и typecheck**

Run: `npm run lint && npm run typecheck`

Expected: обе команды без ошибок, код возврата 0.

- [ ] **Step 5: Commit**

```bash
git add e2e-subpath/smoke.spec.ts
git commit -F - <<'MSG'
test: check offline from the first visit under the Pages base

Workbox прерывает установку service worker, если хоть один URL прекеша
ответил 404, так что под сломанным base эта проверка краснеет сама.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

### Task 4: Джоба e2e-subpath в CI

**Files:**
- Modify: `.github/workflows/ci.yml` (дописать джобу в конец)

**Interfaces:**
- Consumes: npm-скрипт `test:e2e:subpath` из задачи 1.

- [ ] **Step 1: Дописать джобу**

В конец `.github/workflows/ci.yml`, на том же уровне отступа, что `check` и `e2e`:

```yaml

  # Отдельной джобой, а не шагом внутри e2e: идёт параллельно, поэтому общее
  # время CI почти не растёт, и даёт свой красный сигнал — сразу видно, сломан
  # именно продакшен-base или приложение вообще. deploy.yml ждёт заключения
  # всего workflow CI, так что новая проверка автоматически становится ещё
  # одним условием публикации и править deploy.yml не нужно.
  e2e-subpath:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm
      # npm install, а не npm ci — причина выше, в job check.
      - run: npm install --no-audit --no-fund
      - run: npx playwright install --with-deps chromium
      - run: npm run test:e2e:subpath
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          # Имена артефактов в пределах одного запуска обязаны различаться,
          # а джоба e2e уже занимает playwright-report.
          name: playwright-report-subpath
          path: playwright-report/
          retention-days: 7
```

- [ ] **Step 2: Проверить синтаксис YAML**

Run: `node -e "const {readFileSync}=require('node:fs');const s=readFileSync('.github/workflows/ci.yml','utf8');const names=[...s.matchAll(/^  ([a-z0-9-]+):$/gm)].map(m=>m[1]);console.log(names)"`

Expected: `[ 'check', 'e2e', 'e2e-subpath' ]`

- [ ] **Step 3: Убедиться, что deploy.yml не тронут**

Run: `git status --short .github/`

Expected: в выводе только `M .github/workflows/ci.yml`. Строки про `deploy.yml`
быть не должно.

- [ ] **Step 4: Commit**

```bash
git add .github/workflows/ci.yml
git commit -F - <<'MSG'
ci: run the subpath e2e stand as its own job

Отдельная джоба идёт параллельно с e2e и даёт свой сигнал: сразу видно,
сломан продакшен-base или приложение вообще.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

### Task 5: Проверить, что набор действительно краснеет, и прогнать всё

Зелёный набор, который зеленеет и на сломанной конфигурации, бесполезен. Эта
задача — единственное доказательство, что стенд что-то ловит. Правок кода она не
оставляет: каждое временное изменение откатывается внутри задачи.

Два сценария, потому что проверки стерегут разное и одним сломом их не покрыть.

**Files:**
- Временно изменяется и откатывается: `playwright.subpath.config.ts`
- Временно изменяется и откатывается: `e2e-subpath/smoke.spec.ts`

- [ ] **Step 1: Сценарий А — base не применился**

В `playwright.subpath.config.ts` временно заменить строку

```ts
    env: { DEPLOY_BASE: BASE },
```

на

```ts
    env: { DEPLOY_BASE: '/' },
```

Приложение соберётся с корневым base, а тесты по-прежнему будут ждать
`/hanzi-cards/`.

- [ ] **Step 2: Запустить и сверить с ожиданием**

Run: `npm run test:e2e:subpath`

Expected: `2 failed, 2 passed`.

- **Падают** проверка манифеста (`href` равен `/manifest.webmanifest` вместо
  `/hanzi-cards/manifest.webmanifest`) и проверка scope service worker
  (`scope` равен `http://localhost:4174/`).
- **Проходят** проверка без 4xx и проверка офлайна — и это правильно, а не
  дефект. `vite preview` отдаёт `index.html` по любому пути через SPA-фолбэк,
  ассеты и `sw.js` при корневом base лежат в корне и прекрасно находятся, а
  service worker со scope `/` управляет и страницей по `/hanzi-cards/`.
  Эти две проверки стерегут другое: отсутствие битых ссылок и живой офлайн.

Если упала **меньше** чем пара «манифест + scope» — упавшие проверки ничего не
проверяют, их нужно чинить, а не принимать.

- [ ] **Step 3: Откатить сценарий А**

```bash
git checkout -- playwright.subpath.config.ts
git status --short
```

Expected: пустой вывод.

- [ ] **Step 4: Сценарий Б — ловушка с goto**

В `e2e-subpath/smoke.spec.ts`, в тесте «приложение под подпутём грузится без
ответов 4xx», временно заменить

```ts
  const response = await page.goto('./');
```

на

```ts
  const response = await page.goto('/');
```

Это ровно та ошибка, ради которой в тесте стоит проверка `redirectedFrom`.

- [ ] **Step 5: Запустить и убедиться, что ловушка поймана**

Run: `npm run test:e2e:subpath`

Expected: `1 failed, 3 passed` — падает только проверка без 4xx, на
`expect(response.request().redirectedFrom()).toBeNull()`.

Если она **прошла** — защита от ловушки не работает, и `goto('/')` смог бы
уехать в `master` незамеченным. Чинить, пока не покраснеет.

Остальные три проверки проходят, потому что `vite preview` редиректит корень на
подпуть и страница в итоге открывается правильная. Это и есть причина, по которой
одного соглашения «пишем `'./'`» недостаточно, а нужна машинная проверка.

- [ ] **Step 6: Откатить сценарий Б**

```bash
git checkout -- e2e-subpath/smoke.spec.ts
git status --short
```

Expected: пустой вывод. Ни одной изменённой строки остаться не должно.

- [ ] **Step 7: Полный прогон всех проверок проекта**

Run: `npm run lint && npm run typecheck && npm run test && npm run build && npm run test:e2e && npm run test:e2e:subpath`

Expected: все шесть команд зелёные. `npm run test:e2e` — 6 тестов,
`npm run test:e2e:subpath` — 4 теста.

- [ ] **Step 8: Убедиться, что рабочее дерево чистое**

```bash
git status --short
```

Expected: пустой вывод. `dist/` и `dist-subpath/` закрыты игнорами.

- [ ] **Step 9: Код-ревью перед слиянием**

Правило пользователя: в `master` — только после код-ревью. Запустить ревью
диффа ветки относительно `master` и разобрать замечания до слияния.

---

## Итоговое состояние

После задачи 5 в ветке:

- `npm run test:e2e` — 6 тестов при base `/`, как и раньше, конфиг не тронут;
- `npm run test:e2e:subpath` — 4 теста при base `/hanzi-cards/`;
- CI гоняет оба набора параллельными джобами, и оба входят в условие публикации.
